# dev-austausch-serverless

`yarn`

### Start local dynamodb
`sls dynamodb install`
`sls dynamodb start`

### Run everything in offline mode with local DB
`sls offline start`

### View dynamoDB tables (defaults to instance on localhost:8000)
`dynamodb-admin`
